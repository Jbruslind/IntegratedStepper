# integrated-stepper-controller

## Plebian quickstart

Install `pipx`:
```
$ sudo apt install pipx
$ pipx ensurepath
```

Install `pdm`:
```
$ pipx install pdm
```

Install deps:
```
$ pdm install
```

Run controller:
```
$ pdm run controller
```
